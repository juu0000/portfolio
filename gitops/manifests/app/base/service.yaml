# kubernetes/backstage-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: tesst
spec:
  selector:
    app: tesst
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 8080